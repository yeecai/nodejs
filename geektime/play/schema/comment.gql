type Comment {
    id: Int
    avatar: String
    name: String
    isTop: Boolean
    content: String
    publishDate: String
    commentNum: Int
    likeNum: Int
}

type Query {
    comment: [Comment]
}

type Mutation {
    like(id: Int): Int
}